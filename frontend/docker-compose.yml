services:
  frontend:
    build:
      context: .
      target: dev
    volumes:
      - ./src:/app/src
    ports:
      - "3000:3000"
    environment:
      VITE_BACKEND_URL: ""
      VITE_BACKEND_PROXY_URL: http://backend:8001
    depends_on:
      - backend
